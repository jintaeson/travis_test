2language: java

sudo: required

env:
  - ARCH=x64
  - ARCH=arm

before_script:
  - echo "deb http://download.tizen.org/tools/latest-release/Ubuntu_12.04/ /" | sudo tee -a /etc/apt/sources.list
  - sudo apt-get update -qq
  - sudo apt-get install gbs -y
  
script:
  - "bash -ex .travis-ci.sh"
